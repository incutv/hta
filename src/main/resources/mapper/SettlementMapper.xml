<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.settlementsys.mapper.SettlementMapper">

    <insert id="insertSettlement" parameterType="com.example.settlementsys.dto.Settlement">
        INSERT INTO settlements (partner_id, total_amount, status, payment_date, created_at, updated_at)
        VALUES (#{partnerId}, #{totalAmount}, #{status}, #{paymentDate}, now(), now())
    </insert>

</mapper>
